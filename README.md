# UTMKit - پلتفرم ساخت لینک هوشمند و ردیابی UTM

پلتفرم حرفه‌ای برای ساخت لینک‌های کوتاه و ردیابی UTM برای کمپین‌های بازاریابی.

> آخرین به‌روزرسانی: رفع خطاهای TypeScript و مهاجرت به Next.js 16

## 🚀 ویژگی‌ها

- ✅ ساخت لینک‌های کوتاه با پارامترهای UTM
- ✅ داشبورد آمار کامل با نمودارها
- ✅ QR Code برای هر لینک
- ✅ دسته‌بندی لینک‌ها
- ✅ Import/Export دسته‌ای (CSV)
- ✅ رمزگذاری لینک‌ها
- ✅ تاریخ انقضا برای لینک‌ها
- ✅ پنل ادمین حرفه‌ای
- ✅ API کامل برای یکپارچه‌سازی
- ✅ پشتیبانی کامل از RTL و فارسی

## 🛠️ تکنولوژی‌ها

- **Frontend:** Next.js 16, React 19, TypeScript
- **Styling:** Tailwind CSS, shadcn/ui
- **Database:** Prisma ORM, SQLite (قابل تغییر به PostgreSQL)
- **Authentication:** NextAuth.js
- **Charts:** Recharts

## 📦 نصب و راه‌اندازی

### پیش‌نیازها

- Node.js 18+ 
- npm یا yarn

### مراحل نصب

1. **کلون کردن پروژه:**
```bash
git clone <repository-url>
cd Alireza
```

2. **نصب وابستگی‌ها:**
```bash
npm install
```

3. **تنظیم متغیرهای محیطی:**
فایل `.env` را ایجاد کنید و مقادیر زیر را تنظیم کنید:

```env
# Database
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="https://utmkit.ir"
NEXTAUTH_SECRET="your-secret-key-here-change-this-in-production"
```

**برای محیط توسعه:**
```env
NEXTAUTH_URL="http://localhost:3000"
```

4. **راه‌اندازی دیتابیس:**
```bash
npm run prisma:migrate
npm run prisma:seed
```

5. **اجرای پروژه:**
```bash
npm run dev
```

پروژه در آدرس `http://localhost:3000` در دسترس خواهد بود.

## 👤 حساب‌های پیش‌فرض

### حساب ادمین
- **ایمیل:** `admin@example.com`
- **رمز عبور:** `admin123`

### حساب کاربر تست
- **ایمیل:** `test@example.com`
- **رمز عبور:** `123456`

## 📝 اسکریپت‌های مفید

```bash
# اجرای پروژه در حالت توسعه
npm run dev

# ساخت برای production
npm run build

# اجرای production
npm run start

# مدیریت Prisma
npm run prisma:generate    # تولید Prisma Client
npm run prisma:migrate     # اجرای migration
npm run prisma:seed        # seed کردن دیتابیس
npm run prisma:studio      # باز کردن Prisma Studio

# اضافه کردن لینک تست
npm run add:test-link
```

## 🌐 تنظیمات دامنه

دامنه پیش‌فرض: `utmkit.ir`

برای تغییر دامنه، متغیر `NEXTAUTH_URL` در فایل `.env` را به‌روزرسانی کنید:

```env
NEXTAUTH_URL="https://your-domain.com"
```

## 📚 ساختار پروژه

```
├── app/                    # Next.js App Router
│   ├── (admin)/           # صفحات ادمین
│   ├── (dashboard)/       # صفحات داشبورد کاربر
│   ├── (public)/          # صفحات عمومی
│   ├── api/              # API Routes
│   └── l/                # Redirect handler
├── components/            # کامپوننت‌های React
│   ├── admin/            # کامپوننت‌های ادمین
│   ├── analytics/        # کامپوننت‌های آمار
│   ├── links/            # کامپوننت‌های لینک
│   └── ui/               # کامپوننت‌های UI
├── lib/                  # توابع کمکی
├── prisma/               # Prisma schema و migrations
└── public/              # فایل‌های استاتیک
```

## 🔐 امنیت

- تمام مسیرهای ادمین با middleware محافظت شده‌اند
- رمزگذاری رمز عبور با bcrypt
- Session management با NextAuth.js
- محافظت از CSRF

## 📄 لایسنس

این پروژه برای استفاده شخصی و تجاری آزاد است.

## 🤝 مشارکت

برای مشارکت در پروژه، لطفاً یک Issue یا Pull Request ایجاد کنید.

---

**ساخته شده با ❤️ برای جامعه ایرانی**
# utmkit
